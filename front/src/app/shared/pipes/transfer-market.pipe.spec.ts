import { TransferMarketPipe } from './transfer-market.pipe';

describe('TransferMarketPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferMarketPipe();
    expect(pipe).toBeTruthy();
  });
});
